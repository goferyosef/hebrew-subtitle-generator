# Core — required
pysubs2>=1.7.0
deep-translator>=1.11.4
chardet>=5.2.0

# OCR — required for hard-coded subtitle extraction
opencv-python>=4.9.0
pytesseract>=0.3.10

# Auto-sync — required for "Sync Subtitles" feature
ffsubsync>=0.4.26

# Drag & drop — optional (app works fine without it)
tkinterdnd2>=0.3.0
